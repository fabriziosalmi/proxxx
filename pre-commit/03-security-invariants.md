| Invariant (Domain · Description) | Verified E2E | Revisions |
|---|---|---|
| Auth · Passwords and tokens are wiped on drop (zeroize) | ✅ **honest scope**: the wipe is a *compile-time type property*, not a runtime observation — every credential field is a [`SecretString`](src/util/secret.rs) (wraps `Zeroizing<String>`; upstream zeroize guarantees the heap overwrite on drop). Runtime memory inspection after free is UB and deliberately not attempted. Executable pin: `statically_pinned_zeroize_on_drop` ([tests/secret_redaction_test.rs](tests/secret_redaction_test.rs)) fails to compile if the inner storage loses `ZeroizeOnDrop`. Residual field sweep v0.13.2: `TelegramConfig.bot_token`, `PbsClient.auth_header`, SSH passphrases, and the init-wizard blocks were plain `String` — all now `SecretString`. | 6 |
| Auth · Secret-bearing types have a redacting `Debug` (type property, not call-site discipline) | ✅ closed v0.13.2 — `Zeroizing<String>` delegates `Debug` to the inner `String`, so the derived `Debug` on `AuthMethod` / `ProfileConfig` / `PbsConfig` / `TelegramConfig` printed live tokens/tickets/CSRF under any `{:?}` (same class as the AR-7 `WsTarget` leak, which got a hand-written impl in v0.13.1). All credential fields are now [`SecretString`](src/util/secret.rs): `Debug` prints `[REDACTED]` (no length), no `Display` (`format!("{}")` is a compile error — boundaries use explicit `.expose()`), no `Serialize` (cannot ride a state export; aligns with frozen #178). Proofs: sentinel-based `{:?}`/`{:#?}` sweeps in [tests/secret_redaction_test.rs](tests/secret_redaction_test.rs) (full `ProfileConfig` TOML fixture with 5 sentinels, incl. PBS + Telegram) + `debug_redacts_token_variant` / `debug_redacts_password_variant` ([src/api/auth.rs](src/api/auth.rs)); `TicketResponse`/`TicketData` lost their `Debug` derive entirely. | 1 |
| Auth · OS Keychain read occurs on isolated blocking thread (`spawn_blocking`) | ✅ | 5 |
| Auth · SIGHUP config hot-reload swaps live `mcp_token`/`policies`/`rate_limit`; a failed parse keeps last-known-good (AR-1 rotation dependency) | ✅ closed — `reload_once` extracted from the SIGHUP loop ([config/watcher.rs](src/config/watcher.rs)) makes the swap/keep-last-known-good policy unit-testable: `reload_swaps_mcp_token_policies_and_rate_limit` (successful reload swaps all three) + `failed_reload_keeps_last_known_good` (parse error does NOT clear auth). Real-signal end-to-end proof in [tests/config_reload_e2e.rs](tests/config_reload_e2e.rs): a `PROXXX_CONFIG` tempfile + `libc::raise(SIGHUP)` rotates the live token, then a bad edit + SIGHUP is shown to retain the last-known-good. | 1 |
| Auth · Telegram `bot_token` resolves via env / file / inline / keychain hierarchy (no plaintext-only) | ✅ Phase 5.13 — `TelegramConfig::resolve_bot_token` ([config/mod.rs:230](src/config/mod.rs#L230)) mirrors the PVE/PBS pattern: `PROXXX_TELEGRAM_BOT_TOKEN` env > `bot_token_file` (0600 enforced) > inline TOML > OS keychain. `env_var_beats_inline_bot_token` + `bot_token_file_with_lax_permissions_is_refused` ([tests/hitl_e2e.rs](tests/hitl_e2e.rs)) cover precedence + the 0644 footgun. | 1 |
| RBAC · Destructive op by `operator` on unowned VM returns HTTP 403 | ✅ the live-RBAC review wiremock + **live cluster** — `operator_delete_unowned_vm_returns_typed_forbidden` + `operator_stop_unowned_vm_returns_typed_forbidden` ([tests/rbac_e2e.rs](tests/rbac_e2e.rs)) verify proxxx's contract; `operator_bulk_partial_failure_is_deterministic` + `auditor_restart_owned_vm_returns_typed_forbidden` ([tests/rbac_live.rs](tests/rbac_live.rs)) verify the contract holds against real PVE 9.1.1. **Closed a real gap in the process**: POST/PUT/DELETE in [api/client.rs:450,478,495,1496,1709](src/api/client.rs#L450) bailed with plain `anyhow::Error`, masking 403/4xx — only GETs went through `ApiError::from_status`. All 5 sites now produce typed `ApiError::Forbidden`. | 2 |
| RBAC · `operator` cannot view global ACLs/Tokens (returns HTTP 403 / empty) | ✅ the live-RBAC review wiremock + **live cluster** — wiremock: typed-error tests + filtered-200 tests in [tests/rbac_e2e.rs](tests/rbac_e2e.rs). Live: `operator_modify_acl_outside_scope_returns_typed_forbidden` + `auditor_delete_user_returns_typed_forbidden` ([tests/rbac_live.rs](tests/rbac_live.rs)) verify the same contract against real PVE. Operational note: PVEVMAdmin DOES include `Permissions.Modify` within its own scope (operators delegate within /vms/{id}); 403 only fires for paths OUTSIDE the persona's ACL — tests refined to assert at the right scope. | 2 |
| RBAC · Token without Privilege Separation maps to user rights accurately | ✅ the live-RBAC review wiremock + **live cluster** — wiremock: wire-format contract via 5 tests in [tests/rbac_e2e.rs](tests/rbac_e2e.rs). Live: `privsep_tokens_have_own_acl_via_transitive_proof` ([tests/rbac_live.rs](tests/rbac_live.rs)) — fixture creates `privsep=1` tokens with their OWN ACL rows (granted to both user AND token); successful operator restart against PVE 9.1.1 is the load-bearing proof that the privsep wire format is honoured end-to-end. | 2 |
| HITL · Op approved via Telegram but executed by unprivileged user fails | ✅ Phase 5.13 — `pve_403_during_execute_surfaces_as_failure` ([tests/hitl_e2e.rs](tests/hitl_e2e.rs)). Telegram approval does NOT confer extra PVE privilege; mock gateway returning `Err(403)` propagates as `CallbackOutcome::ExecuteFailed { error }` and the daemon answers the callback with `❌ Failed: …`. Refactored `cli::hitl_serve` to dispatch via extracted `hitl::daemon::handle_callback_update` ([src/hitl/daemon.rs](src/hitl/daemon.rs)) so the receiver-side code is testable with `&dyn ProxmoxGateway`. | 1 |
| HITL · `secure_mode` flag prevents bypass of `is_destructive` operations | ✅ Phase 5.13 — `secure_mode_forces_request_approval_for_destructive` ([tests/hitl_e2e.rs](tests/hitl_e2e.rs)). Sender-side gate: when `state.secure_mode = true`, every `delete`/`stop`/`restart`/`migrate` triggers `request_approval` to Telegram. Test asserts via wiremock `Mock::expect(3)` that all 3 destructive ops in the fixture produce a `sendMessage` POST. **Scope honesty (corrected):** `secure_mode` is a **TUI-only** runtime flag ([app.rs:66](src/app.rs#L66), consulted at [tui/mod.rs:1027](src/tui/mod.rs#L1027)); it does NOT gate the MCP transport — that surface is covered by its own fail-closed invariant below. | 2 |
| HITL · Destructive MCP tool without a governing policy is REFUSED (network fail-closed) | ✅ closed — the MCP transport is network-reachable (HTTP, optionally unauthenticated), so a destructive tool (`stop`/`restart`/`delete`/`migrate`/`clone`/`create_guest`) must never execute inline off the MCP path. Prior behaviour was **fail-open**: `check_policies` returns "no match → execute immediately" ([policy.rs:2](src/hitl/policy.rs#L2)), so an operator who never wrote a policy let an unauthenticated caller delete a guest. Now [dispatch.rs](src/mcp/dispatch.rs) REFUSES any destructive tool with no matching policy (typed `isError` envelope, gateway never reached); the only sanctioned route is a matching `[[policies]]` entry → HITL approval → execution by the daemon. Belt-and-braces: `run_http_server` REFUSES to bind a non-loopback interface without `mcp_token` unless `--insecure-bind` is passed ([http_server.rs](src/mcp/http_server.rs)). Tests: `mcp_destructive_without_policy_is_refused`, `mcp_destructive_with_matching_policy_routes_to_approval_not_execution`, `mcp_http_nonloopback_without_token_refuses_start` ([tests/mcp_security_e2e.rs](tests/mcp_security_e2e.rs)) + `is_loopback_bind` unit tests. See [ACCEPTED-RISKS.md](pre-commit/ACCEPTED-RISKS.md) for the residual bearer-token / tag-policy risks. | 1 |
| HITL · Replay attack on Telegram callback data (stale approval) rejected | ✅ Phase 5.13 — `replay_callback_does_not_re_execute` ([tests/hitl_e2e.rs](tests/hitl_e2e.rs)). New `hitl::pending::PendingApprovals` ([src/hitl/pending.rs](src/hitl/pending.rs)) atomically check-and-marks every consumed `txn_id` in a `Mutex<HashSet>`; second delivery of the same callback returns `Replay` and the daemon answers `⚠️ Stale approval — already executed`. Mock gateway confirms `shutdown_guest` was invoked exactly once across two identical callbacks. Threat-model honesty: session-local dedup; cross-restart protection relies on Telegram's `offset` advancement. | 1 |
| Injection · `shell_quote()` escapes `'` and blocks `;`, `$`, `\|`, `\n` in pveum calls | ✅ | 5 |
| Injection · Env var secrets capped at 64 KiB to prevent OOM via ENV  | ✅ `ENV_SECRET_MAX_BYTES = 64*1024` enforced in `env_var_secret` ([config/mod.rs](src/config/mod.rs)); proven by `mod env_secret_cap_tests` (over-cap refused, at-cap accepted, empty→None, normal round-trips) — previously the matrix cited only the constant | 6 |
| Injection · Malicious VM name with ANSI escape codes rendered safely in TUI | ✅ closed Phase 5.13 — `util::sanitize::sanitize_display()` strips C0 (ESC, NL, CR, BEL, BS, …) + DEL; wired at every TUI render of `guest.name`/`tags`/`node` ([guests.rs](src/tui/views/guests.rs), [backup.rs](src/tui/views/backup.rs), [compare.rs](src/tui/views/compare.rs), [heatmap.rs](src/tui/views/heatmap.rs)). 7 unit tests cover ESC[2J repaint, NL row injection, BEL/BS, DEL, unicode pass-through, empty input. Zero-alloc fast path via `Cow::Borrowed` when input is already clean. | 1 |
| Cryptography · SPICE `.vv` file uses `O_EXCL` and `0600` permissions (V2) | ✅ | 3 |
| Cryptography · ISO Download strictly enforces pinned SHA-256 / SHA-512 manifest | ✅ pinned in [iso_library.rs](src/app/iso_library.rs); forwarded to PVE `download-url` via `(algo, hex)` params | 2 |
| Cryptography · TLS validation strictness inherits profile `verify_tls` consistently across PVE REST, PBS, and WebSocket (wsterm) clients | ✅ single source of truth — one `verify_tls` field per profile, propagated to all three transports (`api/client.rs`, `pbs/client.rs`, `ws/client.rs`). Wording corrected from earlier "scoped to WS only" framing which was inaccurate — it's by design uniform. | 6 |
| Cryptography · SSH uses modern KEX/Ciphers, rejects deprecated algorithms (SHA1) | ✅ Phase 5.13 — `hardened_algorithms()` in [ssh/session.rs](src/ssh/session.rs) explicitly whitelists curve25519-sha256 (KEX), ChaCha20-Poly1305 + AES-256-GCM (cipher), HMAC-SHA-512/256-ETM (MAC), Ed25519 (host key). DH-G14, AES-CTR, HMAC-SHA1 all dropped from the negotiated set. Strict-kex extension markers preserved (CVE-2023-48795 / Terrapin). 4 regression tests. | 5 |
| Memory Hygiene · Panic hook flight recorder scrubs secrets before writing to stderr/log | ✅ closed Phase 5.13 — `scrub_secrets()` in [util/panic_hook.rs](src/util/panic_hook.rs) substitutes `[REDACTED]` for value runs of `PVEAuthCookie=`, `PBSAPIToken=`, `tokenid=`, `password=`, `token_secret=`, `csrfpreventiontoken=`, `Authorization: Bearer …`, and Telegram `bot<token>` URLs. Case-insensitive match, UTF-8-safe, applied to BOTH the audit-log payload and the stderr message. 8 unit tests including PVE cookie, PBS token, Telegram bot URL, multi-credential payload, unicode payload. | 4 |
| Memory Hygiene · `exec_guest_command` output containing secrets is not cached in SQLite | ✅ by design: `save_state` ([cache.rs:193](src/app/cache.rs#L193)) writes only `nodes/guests/storage`; `GuestExecResult` never reaches the cache schema | 5 |
| Supply Chain · `cargo audit` in CI enforces 0 CVEs on transitive dependencies  | ✅ | 1 |
| Audit · Chain is admin-verifiable end-to-end and the HMAC key is custody-hardened | ✅ **Verifiability (already strong):** `proxxx audit verify` walks every row, recomputes each MAC under its own chain format (v1/v2), and **exits non-zero on any tamper** ([audit_cmd.rs:64](src/cli/audit_cmd.rs#L64)) — scriptable as `proxxx audit verify \|\| alert`. Exhaustive proptests ([audit/mod.rs](src/audit/mod.rs)) prove any single-column mutation (incl. WHO/WHAT via v2, #173) breaks ≥1 link. New `audit_verify_exit_code_reflects_tampering` ([tests/audit_verify_e2e.rs](tests/audit_verify_e2e.rs)) drives the **real binary**: clean chain → exit 0, one rewritten actor row → exit 1. **Key custody (hardened here):** `load_or_create_key` now **refuses a group/world-readable `audit.key`** on load (unix `0o077` check, mirroring `bot_token_file`), and `PROXXX_AUDIT_KEY` lets an operator relocate the key OFF the DB's volume. Tests: `load_key_rejects_group_or_world_readable_perms`, `audit_key_path_override_takes_precedence`. Residual (co-located default; same-user/root can forge) signed in [ACCEPTED-RISKS.md](pre-commit/ACCEPTED-RISKS.md) AR-5. | 1 |
| Resilience · Operation-queue crash recovery is idempotent (no double-execute on restart) | ✅ closed — the op_queue (TUI) persists intent + status and is re-rendered, not auto-run, on restart; only a user-triggered `ExecuteQueue` dispatches, and it dispatches **ONLY `Pending`** ops ([app.rs:1663](src/app.rs#L1663)) — a restored `Running` op (already dispatched pre-crash) is never re-executed. Closed the crash window with a **write-ahead persist** in `dispatch_side_effect` ([tui/mod.rs](src/tui/mod.rs)): the `Running` status is saved durably (fail-closed — no persist, no dispatch) BEFORE any PVE call, so a crash mid-dispatch leaves the op `Running` (not re-dispatchable), never `Pending` (double-execute). Tests: `execute_queue_dispatches_only_pending_never_restored_running`, `re_executing_after_restore_is_a_noop_no_double_dispatch`, `persist_round_trip_preserves_running_status` ([tests/queue_failsafe_test.rs](tests/queue_failsafe_test.rs)). Residual (sub-window before write-ahead completes; PVE-side reconciliation) signed in [ACCEPTED-RISKS.md](pre-commit/ACCEPTED-RISKS.md) AR-6. | 1 |
| Converge · Unmanned auto-converge blast radius is bounded (mass-delete cannot auto-apply) | ✅ closed — adversarial suite in [daemon.rs](src/cli/daemon.rs) tests, playing the "operator on holiday, bad commit pushed" threat through the real unmanned pipeline (`retain_allowed_families` → `exceeds_unmanned_cap` → `effective_unmanned_prune` → `converge_with_changes(force=false)`) with a `RecordingClient` asserting **zero PVE writes**. Three layers proven: (1) `emptied_repo_mass_wipe_refused_even_without_cap_or_whitelist` — a 60-delete wipe is refused by the Severe bulk gate (≥50) EVEN with both daemon guardrails absent; (2) `sub_severe_delete_flood_slips_preflight_but_cap_catches_it` — pins the 10–49 gap: 30 deletes are Warning-tier so preflight does NOT refuse them (documents the danger), and `max_unmanned_changes` is the defence that does; (3) `unmanned_prune_without_cap_holds_deletes_no_writes` — **fail-closed fix**: `effective_unmanned_prune` holds unmanned deletes unless the operator declares a `max_unmanned_changes` cap, so `converge_prune = true` with no cap now converges non-destructively (creates/updates) but deletes NOTHING. Prior behaviour let a 10–49 delete flood auto-apply. See [ACCEPTED-RISKS.md](pre-commit/ACCEPTED-RISKS.md) AR-4 for the residual (uncapped non-prune converge). | 1 |
