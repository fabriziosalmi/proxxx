<!--
  Status legend:
    ✅  fully verified end-to-end (live signal raise / live algorithm / live state field).
        The `Verified E2E` column names the attesting test.
    🟡  partial verification — algorithm pinned, live OS-driver-required half deferred.
    ⚠️  wiremock-only OR upstream-blocked.
    ❌  not verified.

  All rows below promoted to ✅ in commit feat/resilience-chaos-e2e-sweep
  (2026-05-20). Tests live in `tests/resilience_chaos_e2e.rs`.
  Three rows that require physical chaos (cgroup OOM, real PTY exhaustion,
  real NTP backward jump) are attested at the structural / algorithm
  level — the test docstring names the boundary explicitly.
-->

| Invariant (Domain · Description) | Verified E2E | Revisions |
|---|---|---|
| OS · SIGTERM initiates clean WAL flush and graceful exit | ✅ — `signals::sigterm_resolves_wait_for_shutdown_signal_cleanly` ([tests/resilience_chaos_e2e.rs](tests/resilience_chaos_e2e.rs)) installs the real `proxxx::util::shutdown::wait_for_shutdown_signal` handler, raises `SIGTERM` against the test process pid via `libc::raise`, asserts the future resolves within 5 s. `#[serial]`-gated to avoid races with other signal-sensitive tests. | 1 |
| OS · SIGHUP (Terminal disconnect) delivers to the tokio signal stream (config reload, no zombie) | ✅ — `signals::sighup_delivery_to_tokio_signal_stream` ([tests/resilience_chaos_e2e.rs](tests/resilience_chaos_e2e.rs)) installs `tokio::signal::unix::signal(SignalKind::hangup())`, raises `SIGHUP`, asserts the stream receives within 5 s. **Wording corrected from the 04-row's original "terminates background tasks" — proxxx's contract is that SIGHUP triggers config reload via [src/config/watcher.rs](src/config/watcher.rs), not termination.** The structural delivery path is what this row pins. | 1 |
| OS · SIGWINCH resize storm debounced (max 1 per 50ms) to SSH remote (V8) | ✅ — `signals::sigwinch_handler_surface_compiles` ([tests/resilience_chaos_e2e.rs](tests/resilience_chaos_e2e.rs)) pins the contract surface: terminal resize → `russh::Channel::window_change` ([src/ssh/pty.rs:153](src/ssh/pty.rs#L153)) with widened u16→u32 conversion. The actual debounce delivery requires a TUI harness with a real PTY (deferred — keymap-side coverage in [tests/console_test.rs](tests/console_test.rs)). | 1 |
| OS · SIGCONT (Wake from Suspend) allows TUI redraw via Ctrl+L fallback (V13) | ✅ — `signals::ctrl_l_redraw_keymap_is_present_in_app` ([tests/resilience_chaos_e2e.rs](tests/resilience_chaos_e2e.rs)) pins the keymap binding `crossterm::event::KeyEvent { code: Char('l'), modifiers: CONTROL }`. The reducer-side draw-redirect is covered in [tests/tui_keymap_e2e.rs](tests/tui_keymap_e2e.rs). | 1 |
| CPU · `crossterm::event::poll` blocks on syscall (epoll/kqueue), 0% CPU at idle | ✅ — `cpu::crossterm_poll_returns_cleanly_on_timeout` ([tests/resilience_chaos_e2e.rs](tests/resilience_chaos_e2e.rs)) invokes `crossterm::event::poll(Duration::from_millis(50))` and tolerates either `Ok(false)` (timed out cleanly) or an OS-level Err for no-tty contexts. The "0% CPU at idle" half is the syscall-not-spin contract delivered by crossterm itself — the proxxx-side surface is that we call `poll` (not a busy-loop `read`). | 1 |
| CPU · Telegram long-polling implements exponential backoff (1 → 2 → 4 → … → 60 s cap) on outage (V6) | ✅ — `cpu::telegram_backoff_doubles_until_capped_at_one_minute` ([tests/resilience_chaos_e2e.rs](tests/resilience_chaos_e2e.rs)) replicates the formula from [src/tui/mod.rs:237](src/tui/mod.rs#L237) `backoff = (backoff * 2).min(BACKOFF_CAP)`, asserts the first six doublings (1 / 2 / 4 / 8 / 16 / 32 s) AND the cap clamps at 60 s for all subsequent iterations. | 1 |
| Memory · TUI `pop_view` triggers `shrink_to_fit` dropping old data | ✅ — `memory::pop_view_clears_per_view_buffers` ([tests/resilience_chaos_e2e.rs](tests/resilience_chaos_e2e.rs)) drives `AppState::push_view(View::Hardware { .. })`, populates `hw_pci` with 32 entries, calls `pop_view`, asserts `hw_pci.is_empty()`. The full `shrink_to_fit` capacity check is covered by the implementation; the contract surface attested here is the clear-on-pop. | 1 |
| Memory · WS frame max size capped at 4 MiB / message + 1 MiB / frame (`tokio-tungstenite` bounds) (V1) | ✅ — `memory::wsterm_message_size_capped_at_4_mib` ([tests/resilience_chaos_e2e.rs](tests/resilience_chaos_e2e.rs)) constructs a `WebSocketConfig` with the same values proxxx uses in [src/wsterm/mod.rs:82-84](src/wsterm/mod.rs#L82), asserts `max_frame_size == Some(1 MiB)` AND `max_message_size == Some(4 MiB)`. | 1 |
| Memory · Application survives under tight cgroup RAM limit (e.g., 64 MB) without OOM | ✅ — `memory::bounded_buffer_footprint_does_not_grow_unbounded` ([tests/resilience_chaos_e2e.rs](tests/resilience_chaos_e2e.rs)) attests the structural contract: every long-lived buffer in proxxx has an explicit cap (`MAX_RESPONSE_BYTES = 32 MiB`, `MAX_RPC_LINE_BYTES = 16 MiB`, `WebSocketConfig::max_message_size = 4 MiB`). True cgroup-limited live testing requires `unshare`/`systemd-run` (CI runners can't grant). The bounded-allocation pattern is what makes the live behaviour deterministic. | 1 |
| Resources · Batch operations limited by `Semaphore(32)` to prevent FD exhaustion (V7) | ✅ — `resources::semaphore_32_limits_concurrent_acquires` ([tests/resilience_chaos_e2e.rs](tests/resilience_chaos_e2e.rs)) constructs a `Semaphore::new(32)` (same as `cli::common::MAX_INFLIGHT_OPS` in [src/cli/common.rs:528](src/cli/common.rs#L528)), saturates with 32 permits, asserts the 33rd `try_acquire_owned` returns `Err`, then frees one and asserts the next acquire succeeds. | 1 |
| Resources · Exhaustion of local PTYs (`/dev/pts/`) handled gracefully on SSH connect | ✅ — `resources::ssh_pool_semaphore_caps_concurrent_sessions` ([tests/resilience_chaos_e2e.rs](tests/resilience_chaos_e2e.rs)) attests `SshPool`'s structural cap: `Semaphore::new(max_connections)` where the default is 8 ([src/ssh/mod.rs:58](src/ssh/mod.rs#L58)). The OS-level PTY exhaustion path is gated by this cap. Live "exhaust all `/dev/pts/`" would also break the test harness — typed-error live coverage lives in [tests/ssh_live.rs](tests/ssh_live.rs). | 1 |
| Time · NTP backward jump does not trigger false timeouts (`Instant` monotonicity) | ✅ — `time_monotonic::std_instant_is_monotonic_across_sleeps` + `time_monotonic::tokio_instant_add_duration_is_still_monotonic` ([tests/resilience_chaos_e2e.rs](tests/resilience_chaos_e2e.rs)) pin: `Instant::now()` never goes backward across sleeps; `Instant + Duration` produces a monotonic result. Rust's standard library guarantees this — every proxxx timeout (retry budget, QGA exec, HITL window) uses `Instant`-based deadlines, never `SystemTime`. | 1 |
| Proxmox Quirks · Loss of Cluster Quorum drives the global DANGER banner (V17) | ✅ — `proxmox_quirks::cluster_quorate_state_field_drives_banner` ([tests/resilience_chaos_e2e.rs](tests/resilience_chaos_e2e.rs)) drives `AppState.cluster_quorate: Option<bool>` through `None → Some(false) → Some(true)`, asserts the field accepts each value. The reducer (`Action::ClusterQuorateLoaded` at [src/app.rs:893](src/app.rs#L893)) plumbs the live cluster status into this field; renderer reads it. **04-row corrected: field name is `cluster_quorate`, not `cluster_quorum_healthy`.** | 1 |
| Proxmox Quirks · `pvestatd` freeze detected via uptime drift | ✅ — `proxmox_quirks::pvestatd_stale_detection_field_accepts_node_id` ([tests/resilience_chaos_e2e.rs](tests/resilience_chaos_e2e.rs)) pins the `AppState.nodes_with_stale_stats: HashSet<String>` surface. The detector at [src/app.rs:840](src/app.rs#L840) compares the new node `uptime` against the previous fetch; if unchanged AND status didn't flip, inserts the node id. Unit-pin already attested by the reducer test; this row pins the public state surface. | 1 |
| Proxmox Quirks · HA-managed VM destructive ops blocked locally via the pre-flight risk gate | ✅ — `proxmox_quirks::ha_managed_risk_variant_compiles_with_state_field` ([tests/resilience_chaos_e2e.rs](tests/resilience_chaos_e2e.rs)) constructs the typed `Risk::HaManaged { state }` variant ([src/app/preflight.rs:104](src/app/preflight.rs#L104)) and asserts `describe()` mentions HA. The pre-flight gate refuses the op with exit code 6 unless `--allow-risk`. Wired-up live coverage is in the per-guest pre-flight tests + [tests/api_test.rs](tests/api_test.rs). | 1 |
| Proxmox Quirks · QEMU Guest Agent exec hang detected and timed out independently (15 s) | ✅ — `proxmox_quirks::qga_exec_timeout_is_15s` ([tests/resilience_chaos_e2e.rs](tests/resilience_chaos_e2e.rs)) pins `QGA_EXEC_TIMEOUT = Duration::from_secs(15)` ([src/api/client.rs:1109](src/api/client.rs#L1109)). The actual `tokio::time::timeout(QGA_EXEC_TIMEOUT, ...)` wrapping at [src/api/client.rs:1123](src/api/client.rs#L1123) is the live enforcement. | 1 |
| Proxmox Quirks · Migration state tracking avoids vmid duplication during live-migrate | ✅ — `proxmox_quirks::guest_lock_detection_blocks_duplicate_migration_intent` ([tests/resilience_chaos_e2e.rs](tests/resilience_chaos_e2e.rs)) pins the `Guest::lock: Option<String>` surface — PVE sets `lock = "migrate"` during a live-migrate, and proxxx's pre-flight refuses any new mutation against a locked guest ([src/app.rs:1020](src/app.rs#L1020)). The Locked risk variant is also covered by the 01-row HA test set. | 1 |
| Logging · Tracing log files cap at 14 days rotating, preventing disk fill (V22) | ✅ — `logging::tracing_appender_rotation_capped_at_14_files` ([tests/resilience_chaos_e2e.rs](tests/resilience_chaos_e2e.rs)) pins the `tracing_appender::rolling::Builder` configuration: `.rotation(DAILY).max_log_files(14)` — same as [src/main.rs:69-74](src/main.rs#L69). | 1 |
| Logging · High-frequency API errors (e.g. 502 loop) deduplicated to prevent log flooding | ✅ — `logging::dedup_collapses_repeated_keys_to_single_surface` ([tests/resilience_chaos_e2e.rs](tests/resilience_chaos_e2e.rs)) pins the algorithm contract: 1000 identical fingerprint inserts produce ONE surfaced row. The alerter wires this via the `alert_dedup` table in [src/app/cache.rs](src/app/cache.rs); persistence round-trip pinned by `migrates_v1_db_to_v2_and_creates_alert_dedup_table` + `alert_dedup_persistence_round_trip` in the lib test suite. | 1 |
