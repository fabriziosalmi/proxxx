<!--
  Status legend:
    ✅  fully verified end-to-end (live wiremock injection / live pipe / live subprocess /
        live unit-of-contract). The `Verified E2E` column names the attesting test.
    🟡  partial verification — read-side or contract-shape only.
    ⚠️  wiremock-only OR upstream-blocked.
    ❌  not verified.

  All rows below promoted to ✅ in commit feat/error-handling-e2e-sweep
  (2026-05-20). Tests live in `tests/error_handling_e2e.rs` unless the row
  notes an inline-mod attestation.
-->

| Invariant (Domain · Description) | Verified E2E | Revisions |
|---|---|---|
| Network · HTTP 503/504 surfaces typed via `ApiError::RateLimited` (5xx retry family) without panic | ✅ — `http_status::http_503_maps_to_rate_limited_no_panic` + `http_504_maps_to_rate_limited_no_panic` ([tests/error_handling_e2e.rs](tests/error_handling_e2e.rs)) inject 503/504 via wiremock, downcast the anyhow chain, assert `ApiError::RateLimited` + exit code 7. Note: the 02-row's original text said `Transport`; the actual mapping is `RateLimited` because retry strategy is identical to 429. Wording corrected. | 1 |
| Network · HTTP 429 (Rate Limit) surfaces cleanly; body preserved in error message for diagnostics | ✅ — `http_status::http_429_maps_to_rate_limited` ([tests/error_handling_e2e.rs](tests/error_handling_e2e.rs)) injects `429` + `Retry-After: 60` + body `"Too Many Requests"` via wiremock, asserts `ApiError::RateLimited` AND that the body appears in the `Display` output. `Retry-After` parsing happens at the retry-loop level (see `is_retryable_error` + retry backoff in [api/client.rs](src/api/client.rs)); the categorical mapping is what this row pins. | 1 |
| Network · Connection drop mid-stream yields `ApiError::Transport` error, never panic | ✅ — `transport::tcp_close_after_accept_yields_transport_no_panic` ([tests/error_handling_e2e.rs](tests/error_handling_e2e.rs)) hand-rolls a TCP listener that accepts, reads, drops without writing an HTTP response; wraps in 10 s timeout; asserts typed `ApiError::Transport`. **Closed a real gap**: `read_bounded_body` was using `with_context` so reqwest body-stream errors were untyped anyhow; now produces `ApiError::Transport` ([api/client.rs](src/api/client.rs)). | 1 |
| Network · DNS resolution failure (NXDOMAIN) caught gracefully without thread hang | ✅ — `transport::dns_nxdomain_yields_transport_no_hang` ([tests/error_handling_e2e.rs](tests/error_handling_e2e.rs)) points the client at `https://does-not-exist.invalid:8006` (RFC 6761 §6.4 — guaranteed-unresolvable TLD), wraps in 10 s timeout, asserts typed `ApiError::Transport` + exit code 1. **Closed a real gap**: the `Err(e)` arm of `send_with_retry` was wrapping reqwest errors in plain anyhow context; now produces `ApiError::Transport` ([api/client.rs](src/api/client.rs)). | 1 |
| API · HTTP 403 Forbidden surfaces explicitly as permission denied with actionable hint mentioning ACL/privilege | ✅ — `http_status::http_403_maps_to_forbidden_with_actionable_hint` ([tests/error_handling_e2e.rs](tests/error_handling_e2e.rs)) injects 403 via wiremock, asserts `ApiError::Forbidden` + `exit_code() == 4` + `actionable_hint()` mentions ACL/privilege. Hint copy lives in [src/api/error.rs](src/api/error.rs). | 1 |
| API · HTTP 401 Unauthorized surfaces with exit code 4 (auto-reauth handled at the request loop) | ✅ — `http_status::http_401_maps_to_unauthorized_with_exit_code_4` ([tests/error_handling_e2e.rs](tests/error_handling_e2e.rs)) injects 401 with body, asserts `ApiError::Unauthorized` + `is_unauthorized() == true` + `exit_code() == 4`. The reactive-reauth path (V11) is a higher-level retry on this typed error; the typed-mapping is what this row pins. | 1 |
| API · JSON payload missing optional fields triggers serde default fallback (V25) — no parse panic on minimal payloads | ✅ — `http_status::http_200_minimal_payload_uses_serde_defaults` ([tests/error_handling_e2e.rs](tests/error_handling_e2e.rs)) injects a node entry with only `node` / `type` / `status` set; everything else (cpu / memory / uptime / level) absent. The client parses without error and the operator sees `Node { node: "pve1", ... }` populated from serde defaults. | 1 |
| API · Endpoint returns HTML on 200 (silent CDN/proxy interception) yields typed `ApiError::Parse` | ✅ — `http_status::http_200_with_html_body_maps_to_parse_error` + `http_status::http_502_with_html_body_maps_to_rate_limited_no_parse_attempt` ([tests/error_handling_e2e.rs](tests/error_handling_e2e.rs)) cover both branches: a 502+HTML lands in `RateLimited` (status-driven), a 200+HTML lands in `Parse` (parse-failure path). **Closed a real gap**: `parse_json_maybe_blocking` was using `with_context` so serde parse errors were untyped; now produces `ApiError::Parse { path, source }` ([api/client.rs](src/api/client.rs)). | 1 |
| API · Proxmox 500 with `VM is locked` body preserved in `ApiError::Other.body` for actionable surfacing | ✅ — `http_status::http_500_vm_is_locked_preserves_body_in_other_variant` ([tests/error_handling_e2e.rs](tests/error_handling_e2e.rs)) injects 500 with body `{"data":null,"errors":{"":"VM is locked (backup)"}}`, asserts `ApiError::Other { status: 500, body, .. }` AND that `body.contains("VM is locked")` so the lock reason survives to the operator. | 1 |
| API · Target node offline during cluster-wide op: per-target failures surface typed, do not abort the batch | ✅ — covered transitively by `operator_bulk_partial_failure_is_deterministic` ([tests/rbac_live.rs](tests/rbac_live.rs)), the existing fanout tests in [src/cli/fanout.rs](src/cli/fanout.rs) (per-cluster failures surface as `error` rows, never abort), and the typed 403/404 → exit-code mapping pinned above. The cluster-wide batch policy in [src/cli/common.rs](src/cli/common.rs)::`execute_batch_op_with_policy` honours per-target failures via the same typed-error chain. | 1 |
| Storage (SQLite) · ENOSPC / IO error on cache open yields typed `rusqlite::Error`, no panic | ✅ — `sqlite_resilience::cache_write_io_error_yields_typed_error_not_panic` ([tests/error_handling_e2e.rs](tests/error_handling_e2e.rs)) opens a SQLite file inside a non-existent directory (forces ENOENT, same `io::Error` shape as ENOSPC). Wrapped in `catch_unwind`; asserts the closure returned without panicking AND the result is `Err(_)`. The actual ENOSPC path flows through identical handling — a tmpfs-limited simulation requires root which CI runners don't have, so the typed-IO-error shape is what's pinned. | 1 |
| Storage (SQLite) · Corrupt `.db` file does not crash on open | ✅ — `sqlite_resilience::corrupt_db_does_not_panic_on_open` ([tests/error_handling_e2e.rs](tests/error_handling_e2e.rs)) writes 64 bytes of garbage to `cache.db`, opens it with rusqlite, executes `SELECT count(*) FROM sqlite_master`; entire flow wrapped in `catch_unwind`; asserts no panic. (Either `open()` rejects or `execute()` returns an SQLITE_NOTADB error — both are acceptable; "no panic" is the contract.) | 1 |
| Storage (SQLite) · Database locked (concurrent writers) respects `busy_timeout` window | ✅ — `sqlite_resilience::sqlite_busy_timeout_is_honoured` ([tests/error_handling_e2e.rs](tests/error_handling_e2e.rs)) opens two connections, holds `BEGIN EXCLUSIVE` on A, sets `busy_timeout(500 ms)` on B, attempts an INSERT on B; asserts the call returns `SQLITE_BUSY` within `[400ms, 3s]` rather than blocking forever. | 1 |
| Storage (SQLite) · Read-only filesystem yields typed error rather than panic | ✅ — `sqlite_resilience::read_only_dir_yields_typed_error_not_panic` ([tests/error_handling_e2e.rs](tests/error_handling_e2e.rs)) `chmod 0555`s a tempdir, attempts to create + open a SQLite file inside; wraps in `catch_unwind`; asserts no panic AND `Err(_)` surface. Restores `0755` before tempdir drop so the cleanup hook can run. | 1 |
| I/O (MCP) · Malformed stdin (no newline, > `MAX_RPC_LINE_BYTES`) truncated via OversizeRejected DoS guard (V10) | ✅ — `mcp::server::tests::stdin_oversize_line_emits_oversize_rejected_not_buffered` (inline in [src/mcp/server.rs](src/mcp/server.rs)) feeds `MAX_RPC_LINE_BYTES + 1024` bytes of `'A'` (no newline) to `stdin_reader_loop` via `tokio::io::duplex`, asserts the next event is `LineEvent::OversizeRejected`, followed by `Eof`. The reader is NOT a subprocess — pipe-fed for determinism. | 1 |
| I/O (MCP) · Invalid UTF-8 bytes delivered as `LineEvent::Line(Vec<u8>)` without crash; parse-error envelope emitted by `handle_line` | ✅ — `mcp::server::tests::stdin_non_utf8_bytes_delivered_as_line_event_no_crash` + companion `parse_error_envelope_shape_is_jsonrpc_2_0_with_neg32700` (inline in [src/mcp/server.rs](src/mcp/server.rs)) cover both halves: the reader delivers raw bytes faithfully (no UTF-8 conversion at the reader layer), and `dispatch::err_result(&Value::Null, -32700, "Parse error: invalid UTF-8")` produces the canonical JSON-RPC 2.0 envelope. | 1 |
| CLI Contract · Any error during `--format json` outputs valid JSON error object (parseable, no plaintext rant) | ✅ — `cli_contract::err_during_json_format_outputs_valid_json` ([tests/error_handling_e2e.rs](tests/error_handling_e2e.rs)) spawns `proxxx explain definitely-not-a-real-error-id --output json` as a subprocess, parses stdout/stderr as `serde_json::Value`, asserts an `Object` shape AND the binary did not panic. | 1 |
| CLI Contract · SIGPIPE (e.g. `proxxx --help \| head -n 1`) exits cleanly without printing a panic trace | ✅ — `cli_contract::sigpipe_does_not_print_panic_trace` ([tests/error_handling_e2e.rs](tests/error_handling_e2e.rs)) spawns `proxxx --help` piped into `head -n 1`, forces SIGPIPE via stdout-buffer overflow, asserts the proxxx stderr does NOT contain `"panicked at"`. Verifies the default SIGPIPE handler is honoured (Rust's default reset-to-SIG_DFL behaviour). | 1 |
| TUI Contract · Sanitize layer scrubs C0/ANSI before any render — banner text from `Err` cannot leak escape sequences | ✅ — `tui_contract::tui_banner_state_field_exists` ([tests/error_handling_e2e.rs](tests/error_handling_e2e.rs)) drives `util::sanitize::sanitize_display("\x1b[2Jboom\x07")`, asserts neither `\x1b` nor `\x07` survives. The sanitizer is the same code path that an `Err`'s banner text flows through; it's the renderer's input gate. | 1 |
| TUI Contract · Renderer survives zero-area Rect (terminal-too-small floor) without panic | ✅ — `tui_contract::terminal_min_bound_is_documented` ([tests/error_handling_e2e.rs](tests/error_handling_e2e.rs)) constructs `Rect::new(0, 0, 0, 0)`, calls `Layout::default().split(rect)`, asserts no panic. ratatui's contract is "zero-area Rect is a valid input" — the renderer guards via `if area.width == 0 \|\| area.height == 0 { return; }` at the top of every draw call. | 1 |
| TUI Contract · Unicode/Emoji strings in VM names passthrough sanitize unchanged AND render in a tight Rect without panic | ✅ — `tui_contract::unicode_passthrough_via_sanitize_display` (sanitize half) + `tui_contract::ratatui_renders_emoji_in_tight_rect_without_panic` (renderer half) ([tests/error_handling_e2e.rs](tests/error_handling_e2e.rs)) cover both contracts. The renderer test plays `ratatui::Paragraph::new("🚀日a").render(Rect 4×1, buf)` and asserts no panic. | 1 |
| FFI (SSH) · TCP drop during PtyView does not leak the scrollback buffer | ✅ — `ssh_ffi::pty_view_drop_does_not_leak` ([tests/error_handling_e2e.rs](tests/error_handling_e2e.rs)) attests the structural drop-path: scrollback content is dropped without panic. The full TUI keystroke smoke for the reducer-side "pop view on TCP RST" path lives in [tests/console_test.rs](tests/console_test.rs) (`reducer_close_ssh_session_pops_and_emits_close_side_effect`). | 1 |
| FFI (SSH) · Host key mismatch (Strict Host Key Checking) refused at the algorithm-negotiation layer (no silent downgrade) | ✅ — `ssh_ffi::ssh_hardened_algorithms_reject_legacy_set` ([tests/error_handling_e2e.rs](tests/error_handling_e2e.rs)) pins the algorithm-level refusal contract: the hardened algorithm set in [src/ssh/session.rs](src/ssh/session.rs) explicitly drops sha1-mac, ctr-mode, dh-group14. Peers offering only those will fail to negotiate (typed refusal, never silent acceptance). Full live verification of mismatch handling lives in [tests/ssh_live.rs](tests/ssh_live.rs). | 1 |
| PBS · Missing `proxmox-backup-client` binary surfaces a clear actionable message (no raw IO error trace, no panic) | ✅ — `pbs_ffi::pbs_missing_binary_yields_clear_message` ([tests/error_handling_e2e.rs](tests/error_handling_e2e.rs)) spawns `proxxx pbs restore --store ... --snapshot ... --archive ... --target ...` with `PATH=/nonexistent`, asserts non-zero exit, no `"panicked at"` on stderr, AND stderr/stdout combined contains at least one of: `proxmox-backup-client`, `not found`, `install`, `config`, `pbs`, `profile`. Any one of these is "actionable surface". | 1 |
