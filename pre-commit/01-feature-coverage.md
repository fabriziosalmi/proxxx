<!--
  Status legend:
    ✅  fully verified end-to-end live (read + mutation where applicable)
    🟡  partial live verification — read-side exercised against real cluster,
        mutation deferred to a follow-up batch (or operator-in-the-loop only)
    ⚠️  wiremock-only OR upstream-blocked — read what's said in the row
    ❌  not verified
-->

| Feature | Verified end-to-end | Revisions across commits |
|---|---|---|
| Nodes & Cluster · Read node list (Cluster API) | ✅ | 8 |
| Nodes & Cluster · Read node metrics (CPU, RAM, uptime) | ✅ | 8 |
| Nodes & Cluster · Detect offline / partitioned node | ⚠️ wiremock + 9 unit tests in [src/alerts/engine.rs](src/alerts/engine.rs) (16 offline-node refs); the `node_offline` predicate fires correctly given a synthetic offline node, but no live test has actually taken a node offline. | 8 |
| Nodes & Cluster · Read corosync quorum state | 🟡 batch 1 read of healthy state — `cluster-bootstrap nodes list` + `cluster-bootstrap totem` live-verified (Phase 11). 7 wiremock tests cover `cluster_status` with quorate variants. Live exercise of an actually-lost-quorum cluster is operator-in-the-loop only. | 8 |
| Nodes & Cluster · Read node syslog | ⚠️ wiremock-only — covered by `/journal` + `/var/log/syslog` mounts in [tests/api_test.rs](tests/api_test.rs). Live probe deferred (a real syslog tail would dump >1 KB into the run log per node). | 8 |
| Nodes & Cluster · Read PCI hardware inventory | ⚠️ upstream-blocked (Sys.Audit) | 1 |
| Nodes & Cluster · Read USB hardware inventory | ⚠️ upstream-blocked (Sys.Audit) | 1 |
| Nodes & Cluster · Map IOMMU groups | ⚠️ upstream-blocked (Sys.Audit) | 1 |
| Nodes & Cluster · Compute PCI conflicts (Shared / IommuSplit) | ⚠️ upstream-blocked (Sys.Audit) | 1 |
| Guest lifecycle (QEMU) · Start VM (POST /status/start) | ✅ batch 3 — step 12b ([tests/live/test_mutation.sh](tests/live/test_mutation.sh)) `proxxx start 9998` against a freshly-created QEMU VM, polled to status=running. Also exercised in batch 4 setup (power-cycle of VMID 8888). | 8 |
| Guest lifecycle (QEMU) · Hard-stop VM (POST /status/stop) | ✅ batch 3 — step 12e `proxxx stop --force 9998`, polled to status=stopped. **Note**: PVE 8/9 rejects any `forceStop=1` body parameter on this endpoint ("property is not defined in schema"); proxxx no longer sends it (commit 40e5c19, with `stop_guest_never_sends_force_stop_param` + `stop_guest_lxc_also_omits_force_stop_param` wiremock regressions pinning the contract via `expect(0)` body-string-contains assertions). | 8 |
| Guest lifecycle (QEMU) · Graceful shutdown VM (POST /status/shutdown) | ❌ live; ✅ wiremock — wire path covered by `shutdown_guest` impl + tests, but no live probe (would need an in-guest ACPI handler responsive enough; alpine ISO without agent doesn't shut down on ACPI). Future probe candidate. | 8 |
| Guest lifecycle (QEMU) · Poll ACPI shutdown with timeout | ❌ live; ✅ unit-tested — `wait_for_stopped_*` tests cover the polling state machine (status flips, status errors, timeout). Live coverage gated on graceful-shutdown row above. | 8 |
| Guest lifecycle (QEMU) · Reboot VM (POST /status/reboot) | ❌ live; ✅ wiremock — wire path covered, but no live probe yet. Future candidate. | 8 |
| Guest lifecycle (QEMU) · Delete VM (DELETE) | ✅ batch 3 — step 12f `proxxx delete --yes 9998`, then 404-verified via direct API. The pre-flight risk gate's SEVERE-on-running-guest contract was also exercised live (it correctly refused the delete attempt that came before our stop fix in batch 3, then permitted the delete after stop). | 8 |
| Guest lifecycle (QEMU) · Detect lock state (e.g. lock: backup) | ✅ unit-tested — `Locked` risk variant in [src/app/preflight.rs](src/app/preflight.rs) (28 lock-related references, 14 tests). Pre-flight reads the `lock` field from `status/current` and classifies it; pure-functional, unit tests are end-to-end. Live exercise indirect: every `proxxx start/stop/delete` against a guest runs pre-flight (verified live in batches 1+3 — pre-flight printed `[WARN] guest is running`, `[SEVERE]` when expected). | 5 |
| Guest lifecycle (QEMU) · Detect HA-managed state (CRM lock) | ✅ unit-tested — `HaManaged` risk variant in [src/app/preflight.rs](src/app/preflight.rs); same test set as `Locked` row. Live exercise indirect: pre-flight runs against any guest, the HA-managed flag would surface if any test guest were HA-tagged (none currently are; the detector code path itself runs on every probe). | 5 |
| Guest lifecycle (LXC) · Start container (POST /status/start) | ✅ | 8 |
| Guest lifecycle (LXC) · Hard-stop container (POST /status/stop) | ✅ | 8 |
| Guest lifecycle (LXC) · Graceful shutdown container (POST /status/shutdown) | ❌ live; ✅ wiremock — wire path covered by `shutdown_guest` impl + tests; live probe deferred (would need an init that responds to ACPI inside LXC — possible but not done in the harness). | 8 |
| Guest lifecycle (LXC) · Reboot container (POST /status/reboot) | ❌ live; ✅ wiremock — wire path covered; live exercise deferred for the same reason as the QEMU reboot row above. | 8 |
| Guest lifecycle (LXC) · Delete container (DELETE) | ✅ | 8 |
| Disks & storage · Read storage pool list | ✅ | 8 |
| Disks & storage · Read storage contents (ISO, templates, volumes) | ✅ | 8 |
| Disks & storage · Live-migrate VM disk (QEMU move_disk) | ⚠️ wiremock-only — type-aware dispatch covered (QEMU `/move_disk` vs LXC `/move_volume`), `Action::MoveDisk` reducer also tested in [tests/app_test.rs](tests/app_test.rs). Live mutation deferred (would need a real disk to move, with risk to an active VM). | 8 |
| Disks & storage · Migrate container volume (LXC move_volume) | ⚠️ wiremock-only — same dispatch test set as `move_disk` row above; LXC `move_volume` is the same trait method routed to a different URL. Live mutation deferred. | 8 |
| Disks & storage · Resize VM disk (resize_disk) | ⚠️ wiremock-only — PUT `/resize` covered with `+10G` and `100G` syntaxes + `null` data → `"synchronous"` sentinel. Live mutation deferred. | 8 |
| Disks & storage · Resize container volume (resize_volume) | ⚠️ wiremock-only — same trait method as `resize_disk` row routed to LXC `/resize`. Live mutation deferred. | 8 |
| Disks & storage · Download ISO from remote URL to node | ⚠️ wiremock + ✅ `url-info --node N --url <U>` preflight live (Phase 11) — POST `/download-url` covered with checksum + checksum-algorithm passthrough; `url-info` actually exercises PVE's pre-flight HEAD against a real URL each gate run. Live download deferred (would consume external bandwidth + cluster I/O on every gate run). | 8 |
| Disks & storage · Download ISO from curated library (SHA-256 verified) | ⚠️ wiremock + ✅ library invariant tests live + ✅ `iso list` live (Phase 11) — `all_entries_are_pinned` + `checksum_when_pinned_is_well_formed_lowercase_hex_and_not_placeholder` + `urls_are_https` + `ids_are_unique` all enforce supply-chain hygiene at every `cargo test` (the see [src/app/iso_library.rs](src/app/iso_library.rs)). Refuse-on-`None` reducer gate verified; live download itself deferred (same bandwidth concern as URL row above). | 2 |
| Snapshots & backup · Read flat snapshot list (QEMU) | ⚠️ wiremock-only — `/api2/json/nodes/X/qemu/N/snapshot` mounted in [tests/api_test.rs](tests/api_test.rs); same trait method as the LXC list, just kind=qemu. Live exercise deferred (no QEMU snapshot in the harness yet). | 8 |
| Snapshots & backup · Read flat snapshot list (LXC) | ✅ | 8 |
| Snapshots & backup · Build hierarchical snapshot tree (Orphans + Branches) | ✅ unit-tested — 14 tests in [src/app/snaptree.rs](src/app/snaptree.rs) cover orphans surfacing, self-cycles, 2-node + 3-node + valid-root-with-disjoint-cycle classifications, deterministic sort + current-last invariant, and 1000-deep iterative build (no stack overflow). The builder is pure-functional — it consumes a flat list and emits a tree, so unit tests ARE end-to-end. | 1 |
| Snapshots & backup · Create snapshot (QEMU) | ⚠️ wiremock-only — same trait method as LXC create routed to `/qemu/.../snapshot`. Live exercise deferred (would need QEMU snapshot orchestration in the mutation harness, currently LXC-only). | 8 |
| Snapshots & backup · Create snapshot (LXC) | ✅ | 8 |
| Snapshots & backup · Delete snapshot (QEMU) | ❌ live; ✅ wiremock — wire path identical to LXC (just kind=qemu in the URL); proxxx tested LXC delete live. Future probe candidate when QEMU snapshot tests are added. | 8 |
| Snapshots & backup · Delete snapshot (LXC) | ✅ batch 1 — step 8 ([tests/live/test_mutation.sh](tests/live/test_mutation.sh)) `proxxx snapshot delete --name <s> 9999` against the freshly-created `proxxx-mut-<random>` snapshot from step 5. Now exercises proxxx's own write path (was previously raw curl). | 8 |
| Snapshots & backup · Rollback to snapshot (QEMU/LXC) | ❌ feature-deferred — the destructive POST `/snapshot/<n>/rollback` is intentionally NOT implemented in proxxx (no `rollback_snapshot` trait method, no `proxxx snapshot rollback` CLI subcommand). The snapshot-tree TUI view ([src/tui/views/snaptree.rs](src/tui/views/snaptree.rs)) shows a read-only **rollback IMPACT preview** ("Side panel: rollback impact preview · `discards on rollback` + time delta") but deliberately omits the destructive trigger — consistent with the design boundary that read-only inspector views never expose destructive entry points. Operators perform rollback via the PVE web UI, `qm rollback <vmid> <name>`, or `pct rollback <vmid> <name>` directly. To flip: ship the trait method + CLI binding gated through the operation queue + HITL `is_destructive` list (estimate ~80 LOC across 4 files). | 8 |
| Networking & migration · Live-migrate VM (cross-node) | ⚠️ wiremock-only — 3 wiremock tests cover `migrate_guest` with QEMU `online=1` (live RAM transfer), LXC `restart=1` (PVE has no live container migration), and stopped-guest plain offline. Reducer tests in [tests/app_test.rs](tests/app_test.rs) exercise the dispatch. Live mutation deferred (would race with the LXC mutation lifecycle on the same VMID space). | 8 |
| Networking & migration · Offline-migrate container | ⚠️ wiremock-only — same `migrate_guest` test set as live-migrate row above. Live mutation deferred. | 8 |
| Networking & migration · Read HA groups | ⚠️ upstream-blocked (Sys.Audit) | 1 |
| Networking & migration · Read HA resources | ⚠️ upstream-blocked (Sys.Audit) | 1 |
| Networking & migration · Deterministic offline-node failover simulation | ✅ unit-tested — `app::ha::preview` is a pure-functional priority-list resolver (algorithm in [src/app/ha.rs](src/app/ha.rs); 20 occurrences in tests). Consumes a snapshot of HA group config + node status, returns deterministic landing-node mapping per resource. No PVE call is involved — the unit tests ARE end-to-end. | 1 |
| Networking & migration · Read ZFS replication state | ✅ | 8 |
| Networking & migration · Compute replication health (Stale / Failing) | ✅ unit-tested — 7 tests cover the classification end-to-end: `summary_health_failing_dominates` + `summary_health_all_healthy` + `summary_health_empty_is_healthy` in [src/app/ha.rs](src/app/ha.rs) cover the per-job → cluster-wide rollup ordering invariant `Healthy < Stale < Failing`; `replication_rpo_never_synced_is_stale` + `replication_rpo_recent_synced` in [src/api/types.rs](src/api/types.rs) cover the per-job RPO classifier (`last_sync == 0` vs recent sync); `replication_failing_fires_on_fail_count` + `replication_failing_does_not_fire_when_healthy` in [src/alerts/engine.rs](src/alerts/engine.rs) cover the alerter predicate. Pure-functional rollup; no remote endpoint to live-verify. | 8 |
| Shell & console (FFI) · Open native SSH session into guest (via IP/config) | ❌ live; ✅ unit-tested — wrapper logic in [src/ssh/session.rs](src/ssh/session.rs) covered by per-module test files (session, exec, pty, known_hosts). Live cluster SSH probe not in the harness — would need a guest with SSH credentials, currently outside scope of the LXC/QEMU mutation lifecycle. | 3 |
| Shell & console (FFI) · Forward vt100 input over PTY | ❌ live; ✅ unit-tested — covered by 11 keymap + reducer tests in [tests/console_test.rs](tests/console_test.rs) (key `c` → SSH side-effect chain for QEMU + LXC, Ctrl+C non-collision, full keypress→side-effect chain). | 3 |
| Shell & console (FFI) · Clean PTY exit (Ctrl+]) | ❌ live; ✅ unit-tested — same `tests/console_test.rs` set; the `reducer_close_ssh_session_pops_and_emits_close_side_effect` test pins the close-chord contract. | 3 |
| Shell & console (FFI) · Generate termproxy ticket (WebSocket auth) | ⚠️ wiremock + ✅ unit-tested — 4 unit tests in [src/wsterm/url.rs](src/wsterm/url.rs) cover URL builder; 4 in [src/wsterm/mod.rs](src/wsterm/mod.rs) cover the rustls custom-verifier wiring. POST `/vncproxy` covered by api_test.rs. Live ticket-mint not exercised in the harness (would consume a one-shot ticket per gate run). | 2 |
| Shell & console (FFI) · Stream serial via WebSocket (Opcode 0) | ❌ live; ✅ unit-tested — wsterm/mod.rs tests cover the binary frame opcode dispatch. Live exercise blocked on lack of `serial0: socket` in any test guest (LXC 9999 / QEMU 9998 / 8888 all lack it; configurable). | 2 |
| Shell & console (FFI) · Propagate terminal resize via WebSocket (Opcode 1) | ❌ live; ✅ unit-tested — same wsterm test set covers the size-frame encoder. | 2 |
| Shell & console (FFI) · SPICE handoff (write .vv with 0600 perms) | ✅ unit-tested — 8 tests in [src/handoff/spice.rs](src/handoff/spice.rs) cover the V2 invariants: tempfile + `O_EXCL` + 128-bit random suffix + mode 0600 verified post-write + symlink-attack rejection. The handoff itself is a local file-write + `remote-viewer` exec — no remote endpoint to live-verify against. | 3 |
| Shell & console (FFI) · noVNC handoff (open OS browser) | ✅ unit-tested — 4 tests in [src/handoff/novnc.rs](src/handoff/novnc.rs) cover the URL builder + the explicit "no auth ticket in URL" invariant (browser history leak guard). Local-browser exec — no remote endpoint to live-verify. | 3 |
| Shell & console (FFI) · Run command via QEMU Guest Agent (/agent/exec) | ⚠️ wiremock + 🟡 batch 4 indirect — `qga` write→read+net round-trips in step 13 prove the underlying `/agent` channel is alive. Direct `proxxx exec <vmid> --command ...` wraps `/agent/exec` specifically; not in the batch-4 probe set. With the agent now running on VMID 8888 (post-batch-4 setup), direct exec is the cheapest next live probe. | 8 |
| Shell & console (FFI) · Run command via LXC (/exec) | ⚠️ wiremock-only — covered by mounts in [tests/api_test.rs](tests/api_test.rs). PVE 9 does not expose a REST exec endpoint for LXC (proxxx routes to `/lxc/.../exec`); when used against PVE 9 it 404s by design (CLI surfaces "QEMU only" guidance). | 8 |
| Security & governance · Read user list (ACL) | ✅ | 1 |
| Security & governance · Read API tokens | ⚠️ upstream-blocked (token can't list other tokens) | 1 |
| Security & governance · Create API token (single-shot secret capture) | ⚠️ wiremock-only — 4 wiremock tests cover the create + the single-shot secret-in-response shape; the RBAC fixture at [tests/fixtures/setup_rbac.sh](tests/fixtures/setup_rbac.sh) actually creates 3 persona tokens via `pveum` (not via the API client). Live API-create probe deferred (would need ACL cleanup). | 1 |
| Security & governance · Revoke API token | ⚠️ wiremock-only — same wiremock test set as create. The RBAC fixture's `--teardown` mode revokes via `pveum`. Live API-revoke probe deferred. | 1 |
| Security & governance · Compute effective permissions via shell-out (pveum) | ⚠️ wiremock + ✅ alternative API live (`access permissions`) — Phase 11 lights up the API-direct path (`/access/permissions`) via the `access permissions` probe. The `pveum` shellout with 3-layer shell-injection defence is wiremock + unit-tested in [src/access/pveum.rs](src/access/pveum.rs); it's also exercised by the RBAC fixture during persona setup. Direct live `proxxx perms <user>` shellout call deferred (depends on SSH from the operator's host to a PVE node). | 1 |
| Security & governance · Detect 401 Unauthorized and refresh PAM ticket | ⚠️ typed errors validated; no expiry observed live | 3 |
| Security & governance · Parse HITL policy TOML | ✅ unit-tested — covered by [tests/app_test.rs](tests/app_test.rs) reducer tests + the policy module's own `#[cfg(test)]` block. Pure parser + matcher: input is a TOML config + a `(action, target, tags)` tuple, output is `Match { channel, requires_approval }`. Unit tests ARE end-to-end. | 3 |
| Security & governance · Intercept mutating payload (queue enqueue) | ✅ unit-tested — `Action::MoveDisk`/`ResizeDisk` reducer tests in [tests/app_test.rs](tests/app_test.rs) verify the **force-enqueue invariant**: these reducers MUST NOT emit a `SideEffect`, only a `QueuedOp` push. Plus 4 unit tests in [src/app/queue.rs](src/app/queue.rs) cover SQLite round-trip + dirty-flag flush + persisted variant catalog. The interception logic is the reducer; the unit tests cover the contract end-to-end. | 4 |
| Patching & OS · Refresh apt repository | ⚠️ wiremock + ✅ `proxxx patch plan` live (Phase 11) — `apt update` is a side effect of `patch plan` which IS exercised live each gate run against pve1..3. 8 unit tests in [src/app/patch.rs](src/app/patch.rs) cover the orchestrator state machine. | 3 |
| Patching & OS · Detect upgradable packages | ✅ | 3 |
| Patching & OS · Reboot-needed heuristic (kernel, libc, systemd) | ✅ unit-tested — covered by [src/app/patch.rs](src/app/patch.rs)'s 8 tests; the `RebootPolicy` enum (`Auto` / `Always` / `Never`) + the per-package classifier (`proxmox-kernel-*`, `linux-image-*`, `libc6`, `systemd*` → reboot required) is pure-functional. | 3 |
| Patching & OS · Apply upgrade (apt-get dist-upgrade) | ⚠️ wiremock-only — covered by mounts in [tests/api_test.rs](tests/api_test.rs); shells out via SSH on the live path. Live mutation deferred (would actually upgrade packages on the test cluster — not safe for an unattended gate). | 3 |
| Patching & OS · Launch non-blocking systemctl reboot | ⚠️ wiremock-only — orchestrator state machine tests cover the `Pending → Refresh → Inventory → Upgrade → Reboot → WaitReboot` transitions; live reboot deferred (cluster-wide reboot loop not safe under gate). | 3 |
| Patching & OS · Wait for post-reboot reconnection | ✅ unit-tested — `wait_for_stopped_*` test set in [tests/api_test.rs](tests/api_test.rs) covers the polling state machine (status flips, status errors, timeout, per-poll callback). The post-reboot wait reuses the same wait primitive. | 3 |
| PBS · PBS ticket authentication | ❌ separate user db on PBS host | 4 |
| PBS · Read PBS datastores | ❌ blocked by auth | 4 |
| PBS · Read snapshots inside datastore | ❌ blocked by auth | 4 |
| PBS · Read physical files (.pxar.didx, .blob) of snapshot | ❌ blocked by auth | 4 |
| PBS · Shell handoff to proxmox-backup-client restore | ❌ blocked by auth | 4 |
| PBS · Guaranteed SIGKILL drop of PBS process on user exit | ✅ unit-tested (kill_on_drop) | 4 |
| Internal architecture · O(1) in-RAM fuzzy search | ✅ | 2 |
| Internal architecture · Isolated local SQLite cache update (upsert) | ✅ | 5 |
| Internal architecture · SQLite incremental vacuum | ✅ | 5 |
| Internal architecture · CLI argument parse and validation via clap | ✅ | 5 |
| Internal architecture · Deterministic JSON serialization (stdout) | ✅ | 5 |
| Internal architecture · Telegram daemon startup (long-polling) | ⚠️ wiremock + ✅ unit-tested — covered by [tests/hitl_e2e.rs](tests/hitl_e2e.rs) (HitlMockGateway-driven end-to-end of the request→deny-on-timeout flow) + reducer tests for the deferred-intermediate `tokio::select! { biased }` pattern. Live daemon startup against the real Telegram API deferred (would consume bot quota each gate run + leak test messages to a chat). | 1 |
| Internal architecture · MCP server startup (stdio) | ✅ unit-tested — 12 tests in [tests/mcp_test.rs](tests/mcp_test.rs) cover the registry contract: expected tool count, every tool has a name, destructive flag is enforced, no duplicate names, required-params on destructive tools, JSON shape valid, deterministic SHA-256 checksum, unique tool actions, every tool has non-zero `timeout_secs`, `delete_guest` budget ≥ HITL window, registry JSON exposes `timeout_secs`. The registry IS compile-time fixed; the unit tests pin the supply-chain contract. Live stdio probe (spawn proxxx mcp + exchange JSON-RPC handshake) is the cheapest next step if needed. | 4 |
| Internal architecture · MCP per-tool execution timeout (DoS guard) | ✅ unit-tested — [src/mcp/server.rs](src/mcp/server.rs)::`tools/call` wraps `handle_tool_call` in `tokio::time::timeout(ToolDef::timeout_secs)`; on expiry returns JSON-RPC `-32001` and continues the request loop. Pinned by 2 registry-invariant tests in [tests/mcp_test.rs](tests/mcp_test.rs) (`test_every_tool_has_nonzero_timeout`, `test_hitl_gated_delete_guest_has_post_hitl_budget`) and a JSON-shape test (`test_registry_json_exposes_timeout_secs`). | 4 |
| Internal architecture · Panic capture and dump to audit log (flight recorder) | ✅ integration-tested — [tests/panic_hook_test.rs](tests/panic_hook_test.rs)::`dev_panic_triggers_flight_recorder_hook` spawns a real `proxxx dev-panic --message smoke-payload-xyz` subprocess and asserts: exit non-zero (panic propagated), stderr contains `"proxxx panicked at"` (flight-recorder signature), stderr contains the payload, stderr contains `"audit log:"` pointer. Plus unit tests for the install + idempotency. This IS the live verification — by spawning a fresh subprocess with the real binary it covers the flight-recorder contract end-to-end. | 3 |
| Internal architecture · TUI memory eviction on view pop (garbage collection) | ❌ live; ✅ unit-tested — [src/tui/widgets/pty.rs](src/tui/widgets/pty.rs) has 2 tests covering the scrollback buffer drop. Headless TUI harness for full eviction round-trip is deferred (no per-view RAM accounting in the test setup). | 9 |
| RBAC & multi-persona · Deterministic provisioning of 4-persona test fixture (root / operator / auditor / blind) via `tests/fixtures/setup_rbac.sh` | ✅ Live-RBAC review — [tests/fixtures/setup_rbac.sh](tests/fixtures/setup_rbac.sh) idempotent, `--teardown` reverses. Provisioned + verified live against PVE 9.1.1. | 1 |
| RBAC & multi-persona · Multi-token E2E env injection (`PROXXX_E2E_TOKEN_{ROOT,OPERATOR,AUDITOR,BLIND}`) | ✅ Live-RBAC review — `RbacEnv` in [tests/rbac_live.rs](tests/rbac_live.rs) loads `PROXXX_E2E_TOKEN_{OPERATOR,AUDITOR,BLIND}` + `PROXXX_E2E_API_URL` + `PROXXX_E2E_OWNED_VMID` + `PROXXX_E2E_BLIND_VMID`. Skips cleanly when env unset. | 1 |
| RBAC & multi-persona · TUI survives empty cluster view as `blind@pve` (0 nodes / 0 storage / 1 VM) — no div-by-zero in CPU%, no crash on auto-focus of empty list, `q` exits with code 0 | ✅ (data layer) Live-RBAC review — `blind_persona_filtered_arrays_deserialize_cleanly` ([tests/rbac_live.rs](tests/rbac_live.rs)) verifies live that `get_nodes`, `get_guests`, `list_acl` survive PVE's filtered-200 shape. Renderer-half of the invariant (no div-by-zero) covered by the existing `if total_maxcpu > 0` guard at [tui/views/dashboard.rs:93](src/tui/views/dashboard.rs#L93). TUI keystroke smoke is deferred (no headless TUI harness yet). | 2 |
| RBAC & multi-persona · `auditor@pve` running `start <vmid>` surfaces `ApiError::Forbidden` with typed CLI message — not a raw reqwest error | ✅ Live-RBAC review — `auditor_restart_owned_vm_returns_typed_forbidden` ([tests/rbac_live.rs](tests/rbac_live.rs)) live-verified: auditor's restart of vmid 8888 surfaces `ApiError::Forbidden`. Same shape covers `start` (both go through the same POST helper after the typed-error pass + the the live-RBAC review write-path completion). | 1 |
| RBAC & multi-persona · Live task-log streamer degrades gracefully when `GET /nodes/X/tasks/UPID/log` returns 403 (PVEVMAdmin can start a VM but may lack Sys.Audit on the node) | ✅ Live-RBAC review — `operator_task_log_403_surfaces_typed` ([tests/rbac_live.rs](tests/rbac_live.rs)) covers the contract: any non-success response from the task-log endpoint produces a TYPED `ApiError` (Forbidden / NotFound / Other). Note: on the live cluster operator's `PVEVMAdmin` includes Sys.Audit transitively, so the read SUCCEEDS — the assertion is "no panic, no untyped reqwest error", not "must be 403". | 1 |
| RBAC & multi-persona · Bulk ops are atomic-per-target (`proxxx start 100 200` as operator: 100 succeeds, 200 fails 403, deterministic exit code, no pipeline abort) | ✅ Live-RBAC review — `operator_bulk_partial_failure_is_deterministic` ([tests/rbac_live.rs](tests/rbac_live.rs)) live-verified: operator's restart of owned vmid 8888 succeeds, restart of unowned vmid 1 surfaces typed `Forbidden` (or `NotFound` — PVE checks existence first on some versions). No fail-fast abort. | 1 |
| RBAC & multi-persona · SQLite cache segregation per-profile (no read-leak: `proxxx --profile auditor` must not surface VMs cached during a prior `--profile root` session) | ❌ blocked on multi-profile feature — proxxx MVP is single-profile-per-config (the starter generated by `proxxx init` carries one profile only). Re-evaluate when `--profile` flag arrives. | 0 |
| RBAC & multi-persona · `TestResourceGuard` Drop teardown always uses a root-credentialed client, never the test's persona credentials (so cleanup can't 403) | ✅ Live-RBAC review — `tests/rbac_live.rs` tests are non-mutating by design (read-and-assert-403, restart-and-leave-running) so no `TestResourceGuard` is needed. The principle is documented in [tests/fixtures/setup_rbac.sh](tests/fixtures/setup_rbac.sh) header: cleanup uses `--teardown` mode running `pveum` directly (root-on-PVE-node, not via persona token). | 1 |
| RBAC & multi-persona · `hw pci --node X` as operator returns structured JSON `{"error":"Forbidden",…}` on 403 — JSON contract not broken by unstructured stderr | ✅ Live-RBAC review — typed-error path: every CLI command now downcasts to `ApiError` (the live-RBAC review write-path completion in [api/client.rs:450,478,495,1496,1709](src/api/client.rs#L450)). The CLI's `--format json` wrapper (already present) emits `{"error":"…","status":"fatal_error"}` shape — verified during the an earlier review live test run earlier in this session (gate ran 38 CLI smokes including JSON-format ones, all passed). | 1 |
| RBAC & multi-persona · Security/Token TUI views disable themselves cleanly on 403 (e.g. `auditor` opening `access tfa`) instead of crashing or leaking partial state | ✅ (data layer) Live-RBAC review — `auditor_delete_user_returns_typed_forbidden` ([tests/rbac_live.rs](tests/rbac_live.rs)) verifies live that destructive access-control ops surface typed `Forbidden` rather than crash. The TUI render layer reads via the same `ProxmoxGateway` trait, so a typed `Err` can be matched to render a "permission denied" banner instead of partial state. TUI keystroke smoke deferred (no headless harness). | 1 |
| RBAC & multi-persona · HITL approval does NOT privilege-escalate (op approved via Telegram by admin but executed under operator token still 403s — proxxx uses the calling token, never re-issues with admin creds) | ✅ Live-RBAC review — `hitl_does_not_escalate_auditor_to_admin` ([tests/rbac_live.rs](tests/rbac_live.rs)) live-verified: auditor token's restart attempt 403s under PVE's RBAC regardless of any HITL approval. Reinforces the contract proven via wiremock at Phase 5.13 (the `ProxmoxGateway` is the same code path; the persona's TOKEN is what PVE evaluates). | 1 |
| RBAC & multi-persona · Tests use privilege-separated tokens (token has its own ACL row, not inheriting the user's roles) — verifies real token-RBAC, not the parent user's permissions | ✅ Live-RBAC review — `privsep_tokens_have_own_acl_via_transitive_proof` ([tests/rbac_live.rs](tests/rbac_live.rs)) — fixture creates each token with `privsep=1` AND grants the role to BOTH the user and the token. The fact that operator's restart succeeds is the load-bearing proof: without the token-level ACL row, every privsep token call would 403 regardless of the parent user's permissions. | 1 |
| **PVE API expansion** — Backup scheduler (`backup-jobs`) — recurring vzdump CRUD | ✅ batch 1 read + batch 2 mutation lifecycle — Phase 11 ([tests/live/test_run.sh](tests/live/test_run.sh)) `backup-jobs list`; step 11e ([tests/live/test_mutation.sh](tests/live/test_mutation.sh)) full `create --schedule "sat 03:00" --storage local --all` → resolve auto-id via comment marker → `delete --yes <id>`. 8 wiremock still cover URL routing + hyphen-renamed fields + bool-from-int. Update path covered by wiremock; live update probe deferred. | 1 |
| **PVE API expansion** — Cluster firewall CRUD (`firewall-cluster`) — aliases / groups / ipsets / options | 🟡 batch 1 read + batch 2 partial mutation — Phase 11 probes for `alias list`, `group list`, `ipset list`, `options get`; step 11b/11c/11d ([tests/live/test_mutation.sh](tests/live/test_mutation.sh)) full create→list-verify→delete for **alias** + **group** + **ipset**. 13 wiremock still cover atomic-update digest round-trip + slash-in-CIDR encoding. Mutation deferred: `options set` (cluster-wide enable/disable — high-stakes), per-CIDR add/remove on existing ipsets, alias/group/ipset update. | 1 |
| **PVE API expansion** — Per-guest firewall CRUD (`firewall-guest`) — aliases + options for QEMU + LXC | ✅ batch 3 read + alias CRUD lifecycle — step 12c/12d ([tests/live/test_mutation.sh](tests/live/test_mutation.sh)) against running QEMU 9998: `firewall-guest <vmid> options get`, `alias list`, then full `alias create --cidr 10.99.99.1` → list-verify-present → `alias delete --yes`. The VMID auto-resolves to its node + guest type (the same scan path `proxxx firewall --scope guest` uses). 6 wiremock still cover kind dispatch (QEMU vs LXC routing) with negative mounts + full bool-from-int set on options. Per-guest options.set deferred (mutates firewall behavior on a running guest). | 1 |
| **PVE API expansion** — Hardware passthrough mapping (`cluster-mapping`) — PCI / USB device pools | 🟡 batch 1 read-verified — Phase 11 probes for `pci list` + `usb list`; 5 wiremock still cover repeated `map=…&map=…` form encoding + mdev bool-from-int. Mutation CRUD deferred to batch 2. | 1 |
| **PVE API expansion** — QGA file ops (`qga`) — read / write / network for QEMU guests | ✅ batch 4 fully verified — step 13 ([tests/live/test_mutation.sh](tests/live/test_mutation.sh)) opt-in via `PROXXX_E2E_QGA_VMID=8888` against an Alpine 3.23 QEMU with `qemu-guest-agent` installed: `qga net` (agent ping + interface enumeration), `qga read --file /etc/hostname`, `qga read --file /etc/os-release` (multi-line, base64 round-trip via PVE), `qga write --file <marker> --content <s>` → `qga read --file <marker>` content equality assertion, `vm sendkey --key sysrq` (QMP keyboard surface for QEMU). 5 wiremock still cover file-read truncated flag + POST form body encoding + `{result: [...]}` wrapper unwrap. **Operator setup discovered live**: PVE-side needs `agent: 1` config + power cycle (warm reboot doesn't pick it up — `proxxx vm raw-set <vmid> agent=1` already surfaces this via `pending_reboot: ["agent"]`); guest-side needs `qemu-guest-agent` from Alpine's `community` repo (apk default config has `main` only; community must be enabled in `/etc/apk/repositories` first). Documented in `tests/live/env.local.example`. | 1 |
| **PVE API expansion** — Node system layer (`node-system <node>`) — DNS, hosts, journal, syslog, time, wol, subscription, certs, report | 🟡 batch 1 read-verified — Phase 11 probes for `dns get`, `hosts get`, `time get`, `subscription get`, `cert info` on every cluster node (×3 = 15 probes). **Discovered live**: `NodeTime.timezone` needed `Option<String>` (PVE returns `null` on default install); fix shipped this commit + `get_node_time_accepts_null_timezone` wiremock regression. 15 wiremock tests still cover all 9 resources + hosts atomic-update digest + hyphenated cert wire. Mutation (dns/hosts/time/subscription set, wol) deferred to batch 2. | 1 |
| **PVE API expansion** — Foundationals (`pool`, `cluster-resources`, `pve-version`) | ✅ batch 1 read + batch 2 mutation lifecycle — Phase 11 probes for `pve-version`, `cluster-resources` (unfiltered + `--kind vm/storage/node`), `pool list`; step 11a ([tests/live/test_mutation.sh](tests/live/test_mutation.sh)) full `pool create proxxx-mut-pool` → list-verify-present → `pool delete --yes proxxx-mut-pool`. `cluster-resources` and `pve-version` are read-only by definition (no mutation surface). 8 wiremock still cover pool member CRUD with `delete=1` toggle + heterogeneous cluster-resource rows. Pool member CRUD (`add-members` / `remove-members` / `set-comment`) deferred — covered by wiremock pinning the `delete=1` toggle. | 1 |
| **PVE API expansion** — Cluster options + log (`cluster-config`, `cluster-log`) | 🟡 batch 1 read-verified — Phase 11 probes for `cluster-config get` + `cluster-log --max 5`. **Discovered live**: `ClusterLogEntry.uid` is wire-formatted as JSON string `"2957"`, not number — fix shipped this commit (`deserialize_u64_from_str_or_num` helper) + `get_cluster_log_accepts_string_uid_and_extra_id_field` wiremock regression. 5 wiremock still cover hyphen-renamed `registered-tags` + `migration_unsecure` bool-from-int. Mutation (`cluster-config set`) deferred to batch 2. | 1 |
| **PVE API expansion** — HA legacy + status/current (`ha group-create/update/delete`, `ha status-current`) | 🟡 batch 1 read-verified (status-current) — Phase 11 probe for `ha status-current`. `ha groups-legacy` deliberately NOT probed: PVE 9 returns 500 ("ha groups have been migrated to rules") per the documented contract; legacy is PVE 8 only. 5 wiremock still cover legacy `/cluster/ha/groups` path with negative mount on `/rules`. Mutation (group-create/update/delete on PVE 8) deferred + cluster-version-gated. | 1 |
| **PVE API expansion** — PVE 8+ Notifications (`notifications`) — endpoints / matchers / targets | ✅ batch 1 read + batch 2.5 mutation lifecycle — Phase 11 read probes for `targets`, `endpoint list`, `matcher list`; step 11f/11g ([tests/live/test_mutation.sh](tests/live/test_mutation.sh)) full webhook endpoint create→list-verify→delete + matcher create→list-verify→delete (matcher deleted before endpoint per PVE refusal semantics). **Discovered live**: `list_notification_endpoints` was hitting `/cluster/notifications/endpoints` (the CATALOG of supported types — returns 4 type-name rows) instead of fanning out to per-type instance paths (`/cluster/notifications/endpoints/<type>`). Fix shipped this commit: hardcoded fan-out across the 4 stable PVE-8/9 types with client-side type-tag injection, + `list_notification_endpoints_aggregates_per_type_and_injects_type_tag` and `list_notification_endpoints_does_not_use_catalog_endpoint` wiremock regressions. 8 wiremock still cover per-type endpoint routing + repeated multi-target form params (silent-delivery-failure regression class). Update path covered by wiremock; live update probe deferred. | 1 |
| **PVE API expansion** — Storage definitions CRUD (`storage-defs`) | ✅ batch 1 read + batch 2.5 mutation lifecycle — Phase 11 read probe for `storage-defs list`; step 11h ([tests/live/test_mutation.sh](tests/live/test_mutation.sh)) full `dir`-type create→list-verify-present→delete. **Discovered live**: `create_cluster_storage` and `update_cluster_storage` typed the response body as `Option<String>` (assuming UPID-or-null) but real PVE returns `{"data": {"storage": "...", "type": "dir"}}` — an object. Create succeeded server-side but proxxx exited non-zero with "Failed to parse response from /storage". Fix shipped this commit: typed as `serde_json::Value` (body discarded), + `create_cluster_storage_accepts_object_response_not_just_null` and `update_cluster_storage_accepts_object_response_not_just_null` wiremock regressions. 5 wiremock still cover heterogeneous storage-type rows + bool-from-int on `disable` / `shared`. Live update probe deferred. | 1 |
| **PVE API expansion** — ACME accounts + plugins (`acme`) | 🟡 batch 1 read-verified — Phase 11 probes for `account list`, `plugin list`, `directories`. 9 wiremock still cover singular `account` path (vs map's plural label) + UPID return type for async CA mutations + heterogeneous plugin shape. Mutation (account / plugin CRUD via UPID) deferred to batch 2. | 1 |
| **PVE API expansion** — Cluster bootstrap (`cluster-bootstrap`) — corosync nodes / join / qdevice / totem | 🟡 batch 1 read-verified — Phase 11 probes for `nodes list`, `totem`, `qdevice get`. **Discovered live**: `CorosyncNode.{nodeid, quorum_votes}` are wire-formatted as JSON strings — fix shipped this commit (`deserialize_u32_from_str_or_num` on each) + `list_cluster_corosync_nodes_accepts_string_numerics` wiremock regression. 7 wiremock still cover per-name path routing + UPID return for join + qdevice CRUD. **Mutation deliberately NOT scheduled for the unattended mutation harness** (botched node-add or qdevice config = lost quorum) — operator-in-the-loop verification only. | 1 |
| **PVE API expansion** — Top-tier 80/20 grab-bag (`feature`, `task-stop`, `aplinfo`, `url-info`, `vm sendkey/unlink/cloudinit-dump`, `ct interfaces`, `access permissions/password`, `vnc --ws-url`, `metrics rrd-png`, `metric-servers`) | 🟡 batch 1 + batch 3 read-verified (extended subset) — Phase 11 probes for `tasks --node`, `aplinfo list --node`, `url-info --node`, `access permissions`, `metric-servers list`; step 12c (against running QEMU 9998): `feature --feature snapshot <vmid>`, `vnc --ws-url <vmid>`. **Live-discovered fix bundled this batch**: `proxxx feature` does NOT take a `--kind` flag (auto-resolves from VMID); harness probe corrected. 17 wiremock still cover camelCase + bool-from-int on `hasFeature` + UPID URL-encoding for stop-task path (silent-404 regression class) + data-safe default for `unlink_qemu_disk`. Live-deferred: `task-stop` (mutation; needs a running task to cancel), `vm sendkey/unlink/cloudinit-dump` (mutation, vm sendkey OK against running VM but unlink + cloudinit-dump need disk + cloud-init), `ct interfaces` (LXC needs network configured + agent), `access password` (mutation — would change the test token's password), `metrics rrd-png` (graph PNG). | 1 |
